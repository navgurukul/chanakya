exports.up = async (knex) => {
    await knex.schema.alterTable('main.students_stages', (table) => {
        table.datetime('transition_done_by');
    });
};
exports.down = async (knex) => {
    await knex.schema.alterTable('main.students_stages', (table) => {
        table.dropColumn('transition_done_by');
    });
};